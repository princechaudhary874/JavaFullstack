package utilities_classes;

public class StringTest {

	public static void main(String[] args) {
		//String= set of characters
		//strings are immutable in java because  strings with the same content share storage in a single pool to minimize creating a copy of the same value
		
		//char	charAt(int index)--> return character at given index	
		/*String str1 = new String();
		str1 = "Me`tahorizon";
		char ch = str1.charAt(0);
		System.out.println(ch);
		*/
		
		
	}

}
